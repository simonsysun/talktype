# TalkType bake-off 朗读文本

录三个文件：`clip1.wav`、`clip2.wav`、`clip3.wav`，放到 `docs/bakeoff/audio/` 下。

按平时说话的样子读，不用刻意放慢。语气词（呃、嗯）和说错改口都留着——那是要测的。
每段之间停一两秒再按停，方便剪成三段（或者直接录三个文件）。

---

**clip1（短，约 5-8 秒）**

呃，明天下午三点，把 TalkType 的 API key 发给 Claude，顺便确认一下 GPT-4o 的账单，3.8 GB 那个文件不用再下载了。

**clip2（中，约 10-15 秒）**

我看了财报，这季度增长不错，主要是云业务带起来的。嗯，不对，我说错了，是周二不是周三，OpenRouter 的账单截止到月底，记得用人民币给 DashScope 的账户充值。

**clip3（长，约 20-25 秒）**

上周我们做了 ASR 的对比测试，Qwen3-ASR-Flash 在中文上的词错率大概是 gpt-4o-transcribe 的三分之一。呃，不过那次只用了一段录音，样本太小，所以这周要重新跑一遍，把 TestFlight 的安装包发给测试的人，让他们重点听 xAI 和 Claude 这两个词，cloud 和 Claude 特别容易听混。
